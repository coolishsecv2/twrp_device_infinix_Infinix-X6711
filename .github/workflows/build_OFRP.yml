name: OrangeFox [OFRP]

on:
  workflow_dispatch:
    inputs:
      MANIFEST_BRANCH:
        description: 'OrangeFox Manifest Branch'
        required: true
        default: '12.1'
        type: choice
        options:
        - '12.1'
        - '11.0'
      DEVICE_TREE:
        description: 'Custom Recovery Tree'
        required: true
        default: 'https://github.com/coolishsecv2/twrp_device_infinix_Infinix-X6711'
        type: string
      DEVICE_TREE_BRANCH:
        description: 'Custom Recovery Tree Branch'
        required: true
        default: 'fox-12.1'
        type: string
      DEVICE_PATH:
        description: 'Specify your device path.'
        required: true
        default: 'device/infinix/X6711'
        type: string
      DEVICE_NAME:
        description: 'Specify your Device Codename.'
        required: true
        default: 'X6711'
        type: string
      BUILD_TARGET:
        description: 'Specify your Build Target'
        required: true
        default: 'vendorboot'
        type: choice
        options:
        - boot
        - recovery
        - vendorboot
      LDCHECK:
        description: 'Use LDCHECK'
        required: false
        default: false
        type: boolean
      LDCHECKPATH:
        description: 'Path of blobs to check'
        required: true
        default: 'vendor/bin/mcDriverDaemon'
        type: string

jobs:
  build:
    name: Build OFRP by ${{ github.actor }}
    runs-on: ubuntu-22.04
    if: github.event.repository.owner.id == github.event.sender.id
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write
    outputs:
      start_epoch: ${{ steps.start.outputs.epoch }}
      commit_id: ${{ steps.clone.outputs.commit_id }}
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Build Started
      id: start
      run: |
        echo "BUILD_START_EPOCH=$(date +%s)" >> $GITHUB_ENV
        echo "epoch=$(date +%s)" >> $GITHUB_OUTPUT

    - name: Clean-up Environment
      uses: rokibhasansagar/slimhub_actions@main

    - name: Setup Swap Space
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 16

    - name: Cache Ubuntu packages
      id: ubuntu-cache
      uses: actions/cache@v4
      with:
        path: |
          /var/cache/apt/archives
          /usr/local
          /opt
        key: ubuntu-build-env-${{ runner.os }}-v1
        restore-keys: |
          ubuntu-build-env-${{ runner.os }}-

    - name: Cache CCACHE
      id: ccache-cache
      uses: actions/cache@v4
      with:
        path: ~/.ccache
        key: ccache-${{ runner.os }}-${{ github.event.inputs.MANIFEST_BRANCH }}-v1
        restore-keys: |
          ccache-${{ runner.os }}-

    - name: Setup Build Environment
      run: |
        sudo apt install aria2 -y
        if [ ! -d "${GITHUB_WORKSPACE}/scripts" ]; then
          git clone https://gitlab.com/OrangeFox/misc/scripts
        fi
        cd scripts
        sudo bash setup/android_build_env.sh

    - name: Set-up Manifest
      if: github.event.inputs.MANIFEST_BRANCH == '11.0' || github.event.inputs.MANIFEST_BRANCH == '12.1'
      run: |
        mkdir -p ${GITHUB_WORKSPACE}/OrangeFox
        cd ${GITHUB_WORKSPACE}/OrangeFox
        if [ ! -d "fox_${{ github.event.inputs.MANIFEST_BRANCH }}/build" ]; then
          git config --global user.name "coolishsecv2"
          git config --global user.email "coolishsecv2@users.noreply.github.com"
          git clone https://gitlab.com/OrangeFox/sync.git
          cd sync
          ./orangefox_sync.sh --branch ${{ github.event.inputs.MANIFEST_BRANCH }} --path ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}
        else
          echo "OrangeFox source already present - skipping sync"
        fi

    - name: Clone Device Tree
      id: clone
      run: |
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}
        git clone ${{ github.event.inputs.DEVICE_TREE }} -b ${{ github.event.inputs.DEVICE_TREE_BRANCH }} ./${{ github.event.inputs.DEVICE_PATH }}
        cd ${{ github.event.inputs.DEVICE_PATH }}
        echo "COMMIT_ID=$(git rev-parse HEAD)" >> $GITHUB_ENV
        echo "commit_id=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT

    - name: Apply OrangeFox Source Patches
      run: |
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}/bootable/recovery
        PATCH_DIR="${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}/${{ github.event.inputs.DEVICE_PATH }}/patches"
        if [ -f "${PATCH_DIR}/ofox_startup_reload_fix.patch" ]; then
          git apply --recount -p3 "${PATCH_DIR}/ofox_startup_reload_fix.patch"
          echo "Applied ofox_startup_reload_fix: validate/discard stale themes + skip startup theme reload"
        else
          echo "Warning: ofox_startup_reload_fix.patch not found in device tree patches/ directory."
        fi

    - name: Check Manifest Branch & Set Build Command
      uses: haya14busa/action-cond@v1
      id: fox_branch
      with:
        cond: ${{ github.event.inputs.MANIFEST_BRANCH == '11.0' || github.event.inputs.MANIFEST_BRANCH == '12.1' }}
        if_true: lunch twrp_${{ github.event.inputs.DEVICE_NAME }}-eng && mka adbd ${{ github.event.inputs.BUILD_TARGET }}image
        if_false: lunch omni_${{ github.event.inputs.DEVICE_NAME }}-eng && mka ${{ github.event.inputs.BUILD_TARGET }}image

    - name: Building OrangeFox
      run: |
        export CCACHE_DIR=~/.ccache
        export CCACHE_EXEC=/usr/bin/ccache
        ccache -M 50G || true
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}
        set +e
        source build/envsetup.sh
        export ALLOW_MISSING_DEPENDENCIES=true
        export FOX_BUILD_TYPE="Unofficial"
        set -e
        ${{ steps.fox_branch.outputs.value }}

    - name: Set Build Date
      run: |
        echo "BUILD_DATE=$(TZ=Asia/Manila date +%Y%m%d)" >> $GITHUB_ENV

    - name: Check Artifact Outputs
      run: |
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}
        if [ -f out/target/product/${{ github.event.inputs.DEVICE_NAME }}/OrangeFox*.img ]; then
            echo "CHECK_IMG_IS_OK=true" >> $GITHUB_ENV
            echo "MD5_IMG=$(md5sum out/target/product/${{ github.event.inputs.DEVICE_NAME }}/OrangeFox*.img | cut -d ' ' -f 1)" >> $GITHUB_ENV
        else
            echo "Image output not found."
        fi
        if [ -f out/target/product/${{ github.event.inputs.DEVICE_NAME }}/OrangeFox*.zip ]; then
            echo "CHECK_ZIP_IS_OK=true" >> $GITHUB_ENV
            echo "MD5_ZIP=$(md5sum out/target/product/${{ github.event.inputs.DEVICE_NAME }}/OrangeFox*.zip | cut -d ' ' -f 1)" >> $GITHUB_ENV
        else
            echo "ZIP output not found."
        fi

    - name: Upload to GitHub Release
      if: env.CHECK_IMG_IS_OK == 'true' || env.CHECK_ZIP_IS_OK == 'true'
      uses: softprops/action-gh-release@v1
      with:
        files: |
          OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}/out/target/product/${{ github.event.inputs.DEVICE_NAME }}/OrangeFox*.img
          OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}/out/target/product/${{ github.event.inputs.DEVICE_NAME }}/OrangeFox*.zip
        name: Unofficial OrangeFox for ${{ github.event.inputs.DEVICE_NAME }} // ${{ env.BUILD_DATE }}
        tag_name: ${{ github.run_id }}
        body: |
          Build Branch: ${{ github.event.inputs.MANIFEST_BRANCH }}
          Device Tree: [Device Tree](${{ github.event.inputs.DEVICE_TREE }}/tree/${{ github.event.inputs.DEVICE_TREE_BRANCH }})
          Commit Hash: [`${{ env.COMMIT_ID }}`](${{ github.event.inputs.DEVICE_TREE }}/commit/${{ env.COMMIT_ID }})
          MD5 (img): `${{ env.MD5_IMG }}`
          MD5 (zip): `${{ env.MD5_ZIP }}`

    - name: Run LDCheck
      if: github.event.inputs.LDCHECK == 'true'
      run: |
        cd tools
        mv -n libneeds ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}/out/target/product/${{ github.event.inputs.DEVICE_NAME }}/recovery/root/
        mv -n ldcheck ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}/out/target/product/${{ github.event.inputs.DEVICE_NAME }}/recovery/root/
        cd ${GITHUB_WORKSPACE}/OrangeFox/fox_${{ github.event.inputs.MANIFEST_BRANCH }}/out/target/product/${{ github.event.inputs.DEVICE_NAME }}/recovery/root
        python3 ldcheck -p system/lib64:vendor/lib64:system/lib:vendor/lib -d ${{ github.event.inputs.LDCHECKPATH }}
        echo "Done checking missing dependencies."
      continue-on-error: true

  notify-started:
    name: Notify Telegram (started)
    runs-on: ubuntu-22.04
    steps:
      - name: Send started alert
        env:
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
        run: |
          if [ -z "$TG_BOT_TOKEN" ] || [ -z "$TG_CHAT_ID" ]; then
            echo "Telegram secrets not set. Skipping notification."
            exit 0
          fi
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          START_TS="$(date +"%m/%d/%y %I:%M %p")"
          MSG="$(printf '%b' "<b>⚙️ OrangeFox Build Started</b>\n📋 <b>Workflow:</b> OrangeFox [OFRP]\n📱 <b>Device:</b> Infinix X6711\n🌿 <b>Tree Branch:</b> <code>${{ github.event.inputs.DEVICE_TREE_BRANCH }}</code>\n🧬 <b>Build:</b> <code>${{ github.event.inputs.BUILD_TARGET }}</code> | <code>${{ github.event.inputs.MANIFEST_BRANCH }}</code>\n🔍 <a href=\"${RUN_URL}\">Watch on GitHub Actions</a>\n[${START_TS}]")"
          curl -s -o /dev/null -X POST "https://api.telegram.org/bot${TG_BOT_TOKEN}/sendMessage" -d chat_id="${TG_CHAT_ID}" --data-urlencode text="$MSG" -d parse_mode="HTML" -d disable_web_page_preview=true

  notify-done:
    name: Notify Telegram (done)
    needs: build
    if: always()
    runs-on: ubuntu-22.04
    steps:
      - name: Send completion alert
        env:
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
        run: |
          if [ -z "$TG_BOT_TOKEN" ] || [ -z "$TG_CHAT_ID" ]; then
            echo "Telegram secrets not set. Skipping notification."
            exit 0
          fi
          START_EPOCH="${{ needs.build.outputs.start_epoch }}"
          COMMIT_ID="${{ needs.build.outputs.commit_id }}"
          [ -z "$START_EPOCH" ] && START_EPOCH=$(date +%s)
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          RELEASE_LINK="https://github.com/${{ github.repository }}/releases/tag/${{ github.run_id }}"
          END_TIME=$(date +%s)
          DURATION_SECS=$((END_TIME - START_EPOCH))
          DURATION=$(printf '%02d:%02d:%02d' $((DURATION_SECS/3600)) $(((DURATION_SECS%3600)/60)) $((DURATION_SECS%60)))
          SHORT_COMMIT=$(echo "$COMMIT_ID" | cut -c1-7)
          JOB_STATUS="${{ needs.build.result }}"
          if [ "$JOB_STATUS" = "success" ]; then
            MSG="$(printf '%b' "<b>✅ OrangeFox Build Completed</b>\n📱 <b>Device:</b> Infinix X6711\n⏱ <b>Duration:</b> ${DURATION}\n🧬 <b>Build:</b> <code>${{ github.event.inputs.BUILD_TARGET }}</code> | <code>${{ github.event.inputs.MANIFEST_BRANCH }}</code>\n🔧 <b>Commit:</b> <code>${SHORT_COMMIT}</code>\n📥 <a href=\"${RELEASE_LINK}\">Download from Releases</a>\n🔍 <a href=\"${RUN_URL}\">View on GitHub Actions</a>")"
          else
            MSG="$(printf '%b' "<b>❌ OrangeFox Build Failed</b>\n📱 <b>Device:</b> Infinix X6711\n⏱ <b>Duration:</b> ${DURATION}\n🧬 <b>Build:</b> <code>${{ github.event.inputs.BUILD_TARGET }}</code> | <code>${{ github.event.inputs.MANIFEST_BRANCH }}</code>\n🔧 <b>Commit:</b> <code>${SHORT_COMMIT}</code>\n🔍 <a href=\"${RUN_URL}\">View on GitHub Actions</a>")"
          fi
          curl -s -o /dev/null -X POST "https://api.telegram.org/bot${TG_BOT_TOKEN}/sendMessage" -d chat_id="${TG_CHAT_ID}" --data-urlencode text="$MSG" -d parse_mode="HTML" -d disable_web_page_preview=true
